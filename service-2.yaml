apiVersion: v1
kind: Service
metadata:
  name: fastapi-app-2
  labels:
    app: fastapi-app-2
spec:
  type: LoadBalancer
  selector:
    app: fastapi-app-2
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
